__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/

# Runtime artifacts -- never committed (CLAUDE.md's SPEC-license rule also covers
# anything under results/, which can contain SPEC-derived build/run output).
cfm.db
results/

# scripts/audit_flags_from_spec_results.py's fetch cache -- other parties' published
# SPEC config files, same SPEC-license rule as above; only its derived report is ours.
.cache/
